import { Inter, Lusitana, Raleway } from "next/font/google";

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    weight: ["400"],
    subsets:['latin']
});

export const raleway = Raleway({
    weight: ["500"],
    subsets: ['vietnamese'],
})